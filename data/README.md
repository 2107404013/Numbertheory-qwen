# Data Directory

`data/raw/` 和 `data/processed/` 不进入 Git。

正式评测集会由 Stage 2 生成，默认路径为 `data/processed/public_number_theory_eval.jsonl`。

训练集会由 Stage 4 生成，第一版规模为 5k 数论样本。

不要手动上传大数据、模型权重或 Hugging Face cache 到 GitHub。
